Upcoming Movie HTML Script

Creates an HTML page which displays information and trailers about
upcoming movies.

Running the script:

Make sure you have the 'requests' library for python installed. 
It can be installed using the following command in a terminal 

$ pip install requests

After installing requests run 'main.py' in IDLE


